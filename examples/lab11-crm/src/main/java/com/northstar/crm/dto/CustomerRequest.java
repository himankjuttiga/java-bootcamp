package com.northstar.crm.dto;
/** Inbound create/update payload — not the entity. */
public class CustomerRequest {
    // Stub only (Lab 8). Later labs add inbound fields: fullName, email, etc.
    // No entity import — this is a boundary contract, kept separate from the domain model.
}