package com.northstar.crm.config;
/** Application wiring placeholders — Spring @Configuration arrives later. */
public class AppConfig {
    // Future wiring (Labs 22+): construct and connect beans such as
    // CustomerRepository -> CustomerService -> CustomerController.
    // Spring @Configuration / @Bean methods replace this manual note later.
    // No framework code in Lab 8.
}