package com.northstar.crm.entity;
// Java enum CustomerStatus with exactly four constants for a Northstar CRM
// customer lifecycle: PROSPECT, ACTIVE, SUSPENDED, CLOSED. Plain Java, no annotations.
public enum CustomerStatus {
    PROSPECT,
    ACTIVE,
    SUSPENDED,
    CLOSED
}