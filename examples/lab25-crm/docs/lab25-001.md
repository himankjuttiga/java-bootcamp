# Lab 25 AI review log (`lab25-001`)

- Suggestion accepted: N/A — code authored by hand from the layered starter TODOs.
- Suggestion rejected (and why): N/A — no AI drafts were accepted. Standing rejects if they had appeared: any `ResponseEntity`/HTTP type inside `CustomerService`, any controller import of `CustomerRepository`, field `@Autowired` in place of constructor injection, and premature JPA annotations.
- Or mark N/A if manual only: **Manual only** for this timed path.

## JPA readiness note

`CustomerRepository` is a plain interface (`save` / `findById` / `findAll` / `existsById`). Later it can become `extends JpaRepository<Customer, String>` and `InMemoryCustomerRepository` be replaced by a JPA-backed bean, with **no change** to `CustomerService` or `CustomerController` or the REST routes — the service depends only on the interface seam.
