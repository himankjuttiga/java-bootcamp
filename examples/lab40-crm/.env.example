# Copy to .env (gitignored) and set real lab-only values.
POSTGRES_USER=crm
POSTGRES_PASSWORD=change-me
POSTGRES_DB=crm

SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/crm_lab40
SPRING_DATASOURCE_USERNAME=crm
SPRING_DATASOURCE_PASSWORD=change-me

# Lab-only agent logins. Set real values in .env, which is gitignored.
CRM_AGENT_A_PASSWORD=change-me
CRM_AGENT_B_PASSWORD=change-me

# OWASP Dependency-Check, optional. Never commit the key.
NVD_API_KEY=
