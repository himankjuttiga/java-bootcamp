apiVersion: v1
kind: Service
metadata:
  name: crm-api
  labels:
    app: crm-api
spec:
  type: ClusterIP
  selector:
    app: crm-api
  ports:
    - name: http
      port: 80
      # Aligned with the container port named "http" (8080)
      targetPort: http
