package com.northstar.crm.entity;
/**
 * Domain customer — persistence details arrive in later labs.
 * Future fields: customerId (e.g. CUS-1001), fullName (Amina Khan),
 * email, status (ACTIVE/PROSPECT), createdAt.
 *
 * Kept free of Spring / JPA / Kafka imports (Lab 8 rule).
 */
public class Customer {
    // Intended fields (added in Labs 10+): customerId, fullName, email, status, createdAt.
    // No implementation required yet — this is a compile-ready domain placeholder.
}